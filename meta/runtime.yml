---
# Collections must specify a minimum required ansible version to upload to galaxy
requires_ansible: '>=2.12.0'

# Dependencies on other collections (if any)
# dependencies:
#   ansible.posix:
#     version: ">=1.5.0"

